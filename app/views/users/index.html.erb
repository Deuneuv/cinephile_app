<h1>Liste des utilisateurs</h1>

<table>
  <tr>
    <th>Nom</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.nom %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Montrer', user %></td>
    <td><%= link_to 'Editer', edit_user_path(user) %></td>
    <td><%= link_to 'Détruire', user, :confirm => 'Etes vous sûr de vouloir supprimer l utilisateur?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouvel utilisateur', new_user_path %>
